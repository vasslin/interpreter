# Script Interpreter

Интерпретатор простого скриптового языка Script Interpreter

## Описание

__Script Interpreter__ -  программа, которая позволяет интерпретировать файл с исходным кодом на простом скриптовом языке Script Language с динамической типизацией.
Интерпретатор простого типа, построчно считывающий, анализирующий и обрабатывающий программу с использованием регулярных выражений. Состоит из лексера, разделяющего поток символов на токены, парсера, определяющего структуру в потоке токенов и собирающего ноды в AST, и вспомогательной функции interpret, запускающей ноды в AST.


## Структура проекта.


```
├──  lib/
│    ├── lexer/       
│    │   ├── lexer.h            # объявление класса Lexer
│    │   ├── lexer.cpp          # определение класса Lexer
│    │   ├── token.h            # перечисление типов токенов и определение структуры Token
│    │   └── CMakeLists.txt     # файл для сборки
│    ├── parser/   
│    │   ├── parser.h           # объявление класса Parser  
│    │   ├── parser.cpp         # определение класса Parser
│    │   └── CMakeLists.txt     # файл для сборки
│    ├── nodes/                 # ноды для построения AST
│    │   ├── CMakeLists.txt     
│    │   ├── variable_node.h    # определение ноды, содержащей переменную  
│    │   ├── variable_node.cpp      
│    │   ├── while_node.h       # определение ноды, содержащей цикл while 
│    │   ├── while_node.cpp      
│    │   ├── for_node.h         # определение ноды, содержащей цикл for 
│    │   ├── for_node.cpp 
│    │   ├── function_definition_node.h    # определение ноды, содержащей объявление функции 
│    │   ├── function_definition_node.cpp 
│    │   ├── operation_nodes.h  # определение ноды, содержащей ноды и операции между ними (унарные, бинарные)     
│    │   ├── operation_nodes.cpp       
│    │  ....    
│    │   ├── if_node.h          # определение ноды, содержащей условные операторы
│    │   └──  if_node.cpp      
│    ├── AST/   
│    │   ├── CMakeLists.txt   
│    │   ├── AST.h             # объявление AST
│    │   └── AST.cpp           # определение AST
│    ├── interpreter/   
│    │   ├── CMakeLists.txt   
│    │   ├── interpreter.h     # объявление функции interpret  
│    │   └── interpreter.cpp   # определение функции interpret  
├── bin/
│   ├── CMakeLists.txt   
│   └── main.cpp
│        
└── CMakeLists.txt         
```

### Основные компоненты.
1. _Lexer_

__Lexer__ - класс, разделяющий поток символов на токены.  

2. _Parser_

__Parser__ - класс, определяющий структуру в потоке токенов. При определении структуры использует регулярные выражения (см. пункт "Регулярные выражения")  

3. _AST_

__Abstract syntax tree__ - абстрактное синтаксическое дерево — структура данных, которая представляет структуру программы. AST-дерево содержит узлы и ветви (вершины и дуги). Каждая ветвь связывает один узел с другим. У узла может быть несколько выходящих ветвей, но в каждый узел ведёт не более одной ветви. В проекте представлен в виде класса, содержащего контейнер с нодами. В функции `execute()` посещает все ноды в контейнере.  

4. _Interpreter_

__interpret()__ - вспомогательная функция, объединяющая компоненты 1-3.

### Регулярные выражения
При создании нод класс Parser использует регулярные выражния.
Скриптовый язык является регулярным языком, т.е. программный код представляет из себя последовательность регулярных выражений (Regular Expressions), которые задаются определенными правилами. По этим правилам можно построить узлы абстрактного синтаксического дерева.
Функции prim, factor, term, expr определяют выражение с операцией определенного приоритета:


`prim()` - простейшие выражения/выражение expr() в круглых скобках  

```
 BNF : prim := Num | Bool | String | Variable | (Expression) | List | Function(...) | Function
```

`factor()` - вызов функции / операция [] взятия среза от элемента `prim()`  

```
BNF : factor := prim ((...args...) | [...:...])*
```

`term()` - логическое И, операции умножения, деления, взятия остатка по модулю, возведения в степень элементов `factor()`  

```
BNF : term := factor ((MULT | DIV | MOD | POW | AND) factor)*
```

`expr() `- логическое ИЛИ, операции сложения/вычитания над элементами `term()`

```
BNF : expr := term ((SUM | SUB | OR) term)*
```

## Основной функционал

### Основные правила

- Код регистрозависим
- Пробелы и табуляции могут быть вставлены в любом количестве и не влияют на выполнение программы (в отличие от python)
- Для комментария используется `//`. Комментарии действует до конца строки
- Выражение не переносится на другую строку


### Стандартные типы данных

1. **Числа**
  - Знаковые
  - Двойная точность (соответствует double в языке C++)
  - Специальные литералы для булевой логики: `true` (1) и `false` (0)
  - Поддержка  нотации (например, `1.23e-4`)

2. **Строки**
  - Строковые литералы - всё, что окружено двойными кавычками `""`
  - Escape sequences должны корректно обрабатываться
  - "Some "string" type" не должно интерпретироваться, правильный синтаксис "Some \\"string\\" type"

3. **Списки**
  - Динамические массивы
  - Литералы в квадратных скобках: `[1, 2, 3]`
  - Индексация с нуля
  - Поддержка срезов (slices)

4. **Функции**

5. **NullType**
  - Специальный тип означающий ничего
  - Специальный литерал этого типа `nil`

### Операторы

1. **Арифметические**
  - `+`, `-`, `*`, `/`, `%` (остаток от деления)
  - `^` (возведение в степень)
  - Унарные `+` и `-`

2. **Сравнения**
  - `==`, `!=`, `<`, `>`, `<=`, `>=`

3. **Логические**
  - `and`, `or`, `not`

4. **Присваивания**
  - `=`, `+=`, `-=`, `*=`, `/=`, `%=`, `^=`

5. **Другие**
  - `[]` (индексация)


### Операторы и Стандартные типы данных


1. Числа (применяются в общепринятом понимании)
   - Арифметические
   - Сравнения
   - Логические
   - Присваивания


2. Строки
   - Сравнения (в лексикографическом порядке)
   - Арифметические
       - `+` - конкатенация двух строк
       - `-` - вычитает из строки суффикс (если первый аргумент оканчивается на второй)
       - `*` - применимо к строке и числу, результат - повторяет строку необходимое число раз (не обязательно целое)
   - Оператор `[]`
       - `s[n]` - возвращает n-й символ строки, отрицательные индексы отсчитываются с конца (как в питоне)
       - `s[:n]` - слайс, подстрока от первого до n символа (исключительно)
       - `s[n:]` - слайс, подстрока от n символа до конца
       - `s[m:n]` - слайс, подстрока от m-го символа до n-го(исключительно)
       - `s[:]` - слайс, копия строки
3. Списки
   - Арифметические
       - `+` - конкатенация
       - `*` - повторение (аналогично строке)
   - Оператор `[]`
       - Аналогично строке

4. NullType
   - Может бы сравним (`==`) с переменной любого типа. Возвращает `false` для всех случаем кроме `nil`
   - `!=` имеет обратный результат


### Условные операторы и циклы

1. **Условия**
  ```
  if условие then
      // выражения
  else if условие then
      // выражения
  else
      // выражения
  end if
  ```


2. **Циклы**


- `while`:
   ```
   while условие
       // выражения
   end while
   ```


- `for`:
   ```
   for i in последовательность
       // выражения
   end for
   ```

3. **Break & Continue**
 - Ключевые слова `break` и `continue` имеют привычный смысл при использование в циклах


### Функции


Функции в Script Language являются объектом первого класса, и имеют отдельный тип данных. В отличие от С++, функции в Script Language объявляются через переменную. Для определения функции используется ключевое слово `function`. Количество аргументов не ограничивается. В качестве аргументов могут приниматься любые стандартные типы данных, включая сами функции.


```
имя = function(параметры)
   // выражения
   return результат
end function
```

Вызов функций осуществляется так же, как и в большинстве языков:

```
имя_функции(параметры_через_запятую)
```

Функции в Script Language являются обычным объектом, их можно передавать как аргументы и возвращать. Синтаксис аналогичен другим типам данных:

```
ilovefunctions("pass existing function...", func)

ilovefunctions("...or define one inplace", function(args)
    // do smth
end function)

alias = anotherfunc
```


### Область видимости

Переменные и функции могут быть глобальными (объявлены в глобальной области видимости) и локальные (аргументы функций, локальные переменные)
Локальные переменные одной функции могут попасть в scope другой только через аргументы.
Затемнее внешних переменных может происходить только между глобальными переменными и аргументами функции.


## Стандартная библиотека

### Функции для работы с числами

- `abs(x)` - абсолютное значение
- `ceil(x)` - округление вверх
- `floor(x)` - округление вниз
- `round(x)` - округление до ближайшего целого
- `sqrt(x)` - квадратный корень
- `rnd(n)` - случайное целое от 0 до n-1
- `parse_num(s)`  - преобразует строку в число если возможно, иначе `nil`
- `to_string(n)` - преобразует число в строку


### Функции для работы со строками

- `len(s)` - длина строки
- `lower(s)` - в нижний регистр
- `upper(s)` - в верхний регистр
- `split(s, delim)` - разделение строки
- `join(list, delim)` - объединение списка в строку
- `replace(s, old, new)` - замена подстроки


### Функции для работы со списками

- `range(x, y, step)` - возвращает список чисел `[x; y)` с шагом `step`
- `len(list)` - длина списка
- `push(list, x)` - добавить элемент в конец
- `pop(list)` - удалить и вернуть последний элемент
- `insert(list, index, x)` - вставить элемент
- `remove(list, index)` - удалить элемент
- `sort(list)` - сортировка. Поведение при листе из разных типов -- implementation defined (но не UB!)


### Системные функции

- `print(x)` - вывод в поток вывода без дополнительных символов и перевода строки.
- `println(x)` - вывод в поток вывода с последующим переводом строки.
- `read()` - читает и возвращает строку из потока ввода
- `stacktrace()` - возвращает текущий стэк вызова функций. Формат стэка - на ваше усмотрение.

## Особенности реализации


1. **Динамическая типизация** - типы проверяются во время выполнения.
2. **Автоматическое управление памятью** - сборка мусора, переменные, вышедшие из области видимости, удаляются автоматически.
3. **Лексическая область видимости** - переменные видны в блоке, где объявлены, затемнение внешний имен так же как в С++.
4. **Интерпретация** - выполнение программы происходит построчно, ошибки синтаксиса проверяются в момент выполнения. При возникновении ошибки интерпретатор завершается с ошибкой.
5. **Safety** - выполнение некорректных операций не игнорируется/вызывает ошибки на уровне интерпретатора. Все ошибки Script Language обработаны и пойманы интерпретатором.
6. Простые типы (числа, nil) копируются по значению, сложные (строка, лист, функции) по ссылке. Другими словами, поведение при передаче аргументов и присвоении (`=`) аналогично Python.



## Примеры

В директории `example` есть ряд примеров программ написанных на Script Language 

  - game_guess_the_number.si  

  Мини-игра на двоих на загадывание чисел, реализованная в функции __game_for_two__ : функция принимает число, загаданное первым игроком, второй игрок должен угадать его и ввести свой ответ в консоль. Программа выводит информцию о том, названо загаданное число или нет. Программа выходит из функции, когда число угадано.
  

  ```
  game_for_two = function(num)   
    res = false
    while (!res)
        value = parse_num(read())
        if value == num then
            println("You guessed the number!")
            res = true
        else 
            println("This is not " + to_string(value))
        end if
    end while
    return nil
end function

println("Write number")
num = parse_num(read())

game_for_two(num)
```

  - fibonacci.si

  Программа для поиска n-го числа Фибоначчи.
  Функция fib принимает число n и считает n-е число Фибоначчи.

  ```
  fib = function(n)
    if n == 0 then
        return 0
    end if

    a = 0
    b = 1

    for i in range(n - 1)
        c = a + b
        a = b
        b = c
    end for

    return b
end function


print(fib(10))
```

  - fuzzBuzz.si

```
fizzBuzz = function(n)
    for i in range(1, n)
        s = "Fizz" * (i % 3 == 0) + "Buzz" * (i % 5 == 0)
        if s == "" then
            print(i)
        else
            print(s)
        end if
    end for
end function

fizzBuzz(100)
```
И другие.

## Ограничения
- C++ версии C++23


## Сборка и запуск

1. Клонируйте репозиторий

2. Соберите проект:

```
cd interpreter
mkdir build && cd build
cmake ..
cmake --build .
```

3. Запустите проект:

``` 
cd bin
./script_interpreter
```